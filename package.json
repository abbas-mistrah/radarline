{
  "name": "radarline",
  "version": "1.0.0",
  "description": "Local-first Technology & AI Intelligence OS — from information noise to decision-ready signals.",
  "author": "Abbas Mistrah",
  "license": "MIT",
  "main": "server.js",
  "bin": {
    "radarline": "cli.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test --test-isolation=none",
    "check": "node --check server.js && node --check cli.js && node --check src/database.js && node --check src/scoring.js && node --check src/connectors.js && node --check src/ai.js && node --check src/brief.js && node --check public/app.js",
    "build:demo-db": "node scripts/build-demo-db.js",
    "validate:protocol": "node cli.js check protocol/examples/local-model-release.trace.json"
  },
  "keywords": [
    "technology-intelligence",
    "competitive-intelligence",
    "artificial-intelligence",
    "local-ai",
    "ollama",
    "rss",
    "tech-radar",
    "strategic-foresight",
    "weak-signals",
    "sqlite",
    "pwa",
    "open-source"
  ]
}
